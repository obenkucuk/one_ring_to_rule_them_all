import '../config/app_config.dart';

class HttpUrls {
  static final String baseUrl = AppConfigs.instance.baseUrl;
  static const String unknown = 'api/unknown';
  static const String register = 'auth/register';
}
