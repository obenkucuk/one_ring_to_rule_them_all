import 'package:flutter/material.dart';

class PageBView extends StatelessWidget {
  const PageBView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("PageBView"),
    );
  }
}
